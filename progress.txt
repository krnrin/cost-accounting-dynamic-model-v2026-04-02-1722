# 高压线束精算与决策引擎 — 开发进度日志
# 每个 Claude Code session 完成任务后在此追加记录
# ============================================

## [2026-04-11 02:30] - Task: [T01] Project CRUD + 项目列表页

### 完成的工作:
- 完成项目 CRUD 所依赖的后端链路验证：GET/POST/GET by id/PUT/DELETE `/api/projects` 已通过集成测试
- 重写 `app/src/pages/ProjectListPage.tsx`，实现项目表格视图、搜索、状态筛选、新建/编辑/删除、导入导出与进入项目
- 修复前端项目列表页白屏问题，移除导致类型递归的弹窗表单写法
- 修复 `server/prisma/schema.prisma` 中重复的 `Setting` 模型定义
- 执行 Prisma `db push` 让本地 SQLite 补齐 `Setting` 表，打通 settings 相关接口测试

### 测试:
- 后端：`cd D:/harness-project/server && node test_api.js` → 77 passed, 0 failed
- 前端类型：`cd D:/harness-project/app && npx tsc --noEmit` → 通过
- 前端构建：`cd D:/harness-project/app && npx vite build` → 成功
- 浏览器：Playwright 访问 `http://127.0.0.1:5290/`，验证项目列表页渲染、打开“新建项目”弹窗、点击“进入项目”跳转到 `/project/:id`
- 截图：`test-screenshots/t01-project-list.png`、`test-screenshots/t01-project-create-modal.png`

### 备注:
- `init.ps1` 目前有 PowerShell 语法损坏，未能用于本次初始化，后续 session 应先修复它
- Playwright 控制台剩余的是 React StrictMode 下 Semi UI 的 `findDOMNode` 警告，不影响本任务验收

## [2026-04-11 03:23] - Task: [T02] 项目 Dashboard

### 完成的工作:
- 扩展 `server/src/services/projectService.ts` 的 `GET /api/projects/:id/dashboard` 聚合结果，补齐场景数、内部成本基线、利润差异、分摊总额、已回收金额、回收率、最近报价与场景摘要
- 新增 `app/src/pages/ProjectDashboardOverviewPage.tsx`，实现 `/project/:id` 项目级 Dashboard，展示成本/报价/利润/回收指标卡片与模块导航入口
- 调整 `app/src/App.tsx` 路由：`/project/:id` 现在展示项目 Dashboard，原场景列表迁移到 `/project/:id/scenarios`
- 更新 `app/src/pages/ProjectScenariosPage.tsx` 返回路径，使项目 Dashboard 与场景列表形成双向导航

### 测试:
- 后端：`cd D:/harness-project/server && node test_api.js` → 77 passed, 0 failed
- 前端类型：`cd D:/harness-project/app && npx tsc --noEmit` → 通过
- 前端构建：`cd D:/harness-project/app && npx vite build` → 成功（保留既有 PostCSS `@import` warning，不影响构建）
- 浏览器：Playwright 登录 `http://127.0.0.1:5199/`，验证项目列表点击“进入项目”可进入 `/project/:id` Dashboard，点击“管理场景”跳转到 `/project/:id/scenarios`
- 截图：`project-t02-entered-dashboard.png`、`project-t02-scenarios-from-dashboard.png`

### 备注:
- 直接访问旧示例路径 `/project/e281-quote` 会因旧本地数据不匹配显示“项目不存在”，本任务按真实项目列表点击链路验收通过
- Playwright 中 5173 端口被旧 dev server 占用，本次浏览器验收使用新启动的 5199 端口

## [2026-04-11 03:53] - Task: [T03] 场景 CRUD + 场景列表

### 完成的工作:
- 扩展 `server/src/routes/scenarios.ts`，补齐 `GET /api/scenarios/:sid` 与 `PUT /api/scenarios/:sid` 顶层路由，和任务定义保持一致
- 重写 `app/src/pages/ProjectScenariosPage.tsx`，改为直接对接后端场景 API，完成场景列表、新建场景、编辑场景、来源场景/比较基线展示
- 补充 `/project/:id/scenario/:sid` 路由别名，并将场景列表“进入”按钮统一跳转到该详情路径，满足 T03 验收口径
- 将场景 API 返回结果同步回本地 Dexie，避免项目级页面与场景级页面数据割裂

### 测试:
- 后端：`cd D:/harness-project/server && node test_api.js` → 77 passed, 0 failed
- 前端构建：`cd D:/harness-project/app && npm run build` → 成功（保留既有 PostCSS `@import` warning，不影响构建）
- 浏览器：Playwright 登录 `http://127.0.0.1:5201/`，进入项目 Dashboard → 管理场景 → 新建“客户定点场景”，验证列表渲染、类型切换、创建成功、进入 `/project/:id/scenario/:sid`
- 截图：`test-screenshots/t03-scenarios-created.png`、`test-screenshots/t03-scenario-detail.png`

### 备注:
- 当前验收创建的是无来源场景，因此列表显示为“根场景”；后续做 T04 时可继续利用 `sourceScenarioId` / `compareBaselineId` 完善克隆与比较链路
- 浏览器控制台仍有既有页面错误/告警，但不影响本任务 CRUD 与跳转验收
