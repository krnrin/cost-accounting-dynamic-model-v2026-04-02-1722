# 高压线束精算与决策引擎 — 开发进度日志
# 每个 Claude Code session 完成任务后在此追加记录
# ============================================

🚫 任务阻塞 - 需要人工介入

当前任务: [T14] 系统设置 — 成本结构 + 金属价格 + 费率

已完成的工作:
- 后端已补齐设置接口能力：`GET /api/settings`、`GET /api/settings/:category`、`PUT /api/settings/:category/:key`、`POST /api/settings/publish`、`GET /api/settings/history`、`GET /api/settings/snapshot/:version`
- 后端 `settingsService` 已支持默认分类数据、发布快照版本、历史记录与快照查询
- 前端 `app/src/pages/SettingsPage.tsx` 已改为接入后端设置 API，并支持保存后自动发布快照
- 新增前端 `app/src/lib/settingsApi.ts` 统一访问设置接口
- 已同步 task.json 中历史已提交任务的状态，避免继续重复执行旧任务

阻塞原因:
- 前端构建被现有工程配置阻塞：`npm --prefix D:/harness-project/app run build` 在 Vite/PWA 阶段报错，存在非法输出路径 `../../成本核算动态模型/app/index.html`，不是本次 T14 改动直接引入的问题
- 后端数据库初始化被本机文件占用阻塞：`prisma generate` 在重命名 `query_engine-windows.dll.node` 时触发 `EPERM`，当前无法完成要求中的后端测试链路
- 前端测试也存在项目既有环境问题：缺失 `src/test/setup.ts` 与 `fake-indexeddb/auto`，导致整套 `vitest run` 无法全绿

需要人工帮助:
1. 清理或修复前端现有 Vite/PWA 输出路径配置，消除非法 `index.html` 输出名问题
2. 释放 Prisma 客户端 DLL 占用，或关闭相关进程后重新执行 server 侧 prisma generate/push/seed
3. 补齐前端测试基础依赖与 `src/test/setup.ts`，使现有测试环境可运行

## [2026-04-11 07:24] - Task: [T01] Project CRUD + 项目列表页

### 完成的工作:
- 校验并确认 T01 的项目 CRUD 接口与项目列表页主链路已接通，项目列表支持搜索、状态筛选、删除和进入项目
- 新增 `server/tsconfig.json`，补齐后端 TypeScript 构建配置，保证 server 可正常编译输出 dist
- 修复后端现有路由中的 TypeScript 类型问题，覆盖 `allocations.ts`、`changes.ts`、`tracking.ts`、`quotes.ts` 与 `extraServices.ts`
- 调整 `app/src/index.css` 的字体 `@import` 顺序，消除前端 build 的 PostCSS 告警
- 为 `app/src/pages/ProjectListPage.tsx` 的搜索框补充回车触发刷新，提升项目搜索交互完整性

### 测试:
- `cd D:/harness-project/server && npm run build` 通过
- `cd D:/harness-project/app && npm run build` 通过
- 浏览器打开 `http://127.0.0.1:5173`，验证项目列表页正常渲染并截图 `t01-project-list-page.png`
- Playwright 验证项目搜索可输入 `E281`，并成功进入项目页 `http://127.0.0.1:5173/project/e281-quote`，截图 `t01-project-dashboard.png`
- `http://127.0.0.1:3001/health` 返回 `{\"status\":\"ok\"...}`

### 备注:
- `init.ps1` 当前存在编码/字符串解析错误，未能直接用于初始化；本次改为手动启动并验证前后端
- 浏览器控制台仍有 Semi UI 的 `findDOMNode` StrictMode 警告，但不影响 T01 功能验收

## [2026-04-11 07:24] - Task: [T01] Project CRUD + 项目列表页

### 完成的工作:
- 校验并确认 T01 的项目 CRUD 接口与项目列表页主链路已接通，项目列表支持搜索、状态筛选、删除和进入项目
- 新增 `server/tsconfig.json`，补齐后端 TypeScript 构建配置，保证 server 可正常编译输出 dist
- 修复后端现有路由中的 TypeScript 类型问题，覆盖 `allocations.ts`、`changes.ts`、`tracking.ts`、`quotes.ts` 与 `extraServices.ts`
- 调整 `app/src/index.css` 的字体 `@import` 顺序，消除前端 build 的 PostCSS 告警
- 为 `app/src/pages/ProjectListPage.tsx` 的搜索框补充回车触发刷新，提升项目搜索交互完整性

### 测试:
- `cd D:/harness-project/server && npm run build` 通过
- `cd D:/harness-project/app && npm run build` 通过
- 浏览器打开 `http://127.0.0.1:5173`，验证项目列表页正常渲染并截图 `t01-project-list-page.png`
- Playwright 验证项目搜索可输入 `E281`，并成功进入项目页 `http://127.0.0.1:5173/project/e281-quote`，截图 `t01-project-dashboard.png`
- `http://127.0.0.1:3001/health` 返回 `{\"status\":\"ok\"...}`

### 备注:
- `init.ps1` 当前存在编码/字符串解析错误，未能直接用于初始化；本次改为手动启动并验证前后端
- 浏览器控制台仍有 Semi UI 的 `findDOMNode` StrictMode 警告，但不影响 T01 功能验收

## [2026-04-11 08:29] - Task: [T14] 系统设置 — 成本结构 + 金属价格 + 费率

### 完成的工作:
- 确认后端设置接口已完整覆盖 `GET /api/settings`、分类读取、单项更新、发布、历史与快照查询，并由 `server/src/routes/settings.ts` + `server/src/services/settingsService.ts` 落地
- 完成前端 `app/src/pages/SettingsPage.tsx` 与 `app/src/lib/settingsApi.ts` 的设置页对接，支持基础费率、金属价格、预警阈值、多工厂、分摊配置、BOM 分类的统一加载与保存
- 补齐设置页“发布流”UI，展示全局基准 → 发布快照 → 项目/场景引用，以及发布历史和快照详情
- 清理设置页未使用代码，修复 TypeScript 构建错误，确保前端 build 可通过

### 测试:
- `cd D:/harness-project/server && npm run build` 通过
- `cd D:/harness-project/server && npm run prisma:generate` 通过
- `cd D:/harness-project/app && npm run build` 通过
- `cd D:/harness-project/app && npm run test:run` 未全绿：现有工程存在与本任务无关的失败（`src/data/scenarioFork.test.ts` 的 `getScenarioOnetimeCostFallback` 导出不匹配；多项 UI 测试因 Vitest 将 `src/test/setup.ts` 解析到旧路径 `D:/成本核算动态模型/app/src/test/setup.ts` 失败）
- Playwright 打开 `http://127.0.0.1:4173/settings`，验证设置页已渲染，页面包含“保存配置并发布”“基础配置”等 UI

### 备注:
- `init.ps1` 在当前仓库仍有编码/解析问题，未能直接使用；本次改为手动启动 server 与 preview 服务
- `npm run dev` 当前会因 Vite 依赖重优化异常崩溃，需后续排查；本次用 `vite preview` 完成页面验收

## [2026-04-11 08:29] - Task: [T14] 系统设置 — 成本结构 + 金属价格 + 费率

### 完成的工作:
- 确认后端设置接口已完整覆盖 `GET /api/settings`、分类读取、单项更新、发布、历史与快照查询，并由 `server/src/routes/settings.ts` + `server/src/services/settingsService.ts` 落地
- 完成前端 `app/src/pages/SettingsPage.tsx` 与 `app/src/lib/settingsApi.ts` 的设置页对接，支持基础费率、金属价格、预警阈值、多工厂、分摊配置、BOM 分类的统一加载与保存
- 补齐设置页“发布流”UI，展示全局基准 → 发布快照 → 项目/场景引用，以及发布历史和快照详情
- 清理设置页未使用代码，修复 TypeScript 构建错误，确保前端 build 可通过

### 测试:
- `cd D:/harness-project/server && npm run build` 通过
- `cd D:/harness-project/server && npm run prisma:generate` 通过
- `cd D:/harness-project/app && npm run build` 通过
- `cd D:/harness-project/app && npm run test:run` 未全绿：现有工程存在与本任务无关的失败（`src/data/scenarioFork.test.ts` 的 `getScenarioOnetimeCostFallback` 导出不匹配；多项 UI 测试因 Vitest 将 `src/test/setup.ts` 解析到旧路径 `D:/成本核算动态模型/app/src/test/setup.ts` 失败）
- Playwright 打开 `http://127.0.0.1:4173/settings`，验证设置页已渲染，页面包含“保存配置并发布”“基础配置”等 UI

### 备注:
- `init.ps1` 在当前仓库仍有编码/解析问题，未能直接使用；本次改为手动启动 server 与 preview 服务
- `npm run dev` 当前会因 Vite 依赖重优化异常崩溃，需后续排查；本次用 `vite preview` 完成页面验收


## [2026-04-11 09:38] - Task: [T15] Simulation 仿真引擎

### 完成的工作:
- 后端新增 Simulation/AnnualDrop 数据模型与接口：`server/prisma/schema.prisma`、`server/src/routes/simulations.ts`、`server/src/routes/annualDrops.ts`
- 新增 `server/src/services/simulationService.ts`，支持仿真任务保存、运行结果快照、转正式场景
- 新增 `server/src/services/annualDropService.ts`，支撑年降记录保存与 impact 查询
- 前端新增 `app/src/lib/simulationApi.ts` 统一访问仿真/年降接口
- 重写 `app/src/pages/SimulationPage.tsx`，补齐保存仿真、已保存任务列表、运行任务、转正式场景 UI
- 重写 `app/src/pages/AnnualDropPage.tsx`，补齐保存年降记录、已保存记录列表、impact 载入能力
- 接入 `server/src/index.ts` 路由注册，并完成 Prisma schema push

### 测试:
- `npm --prefix D:/harness-project/server run prisma:generate` 通过
- `npm --prefix D:/harness-project/server run prisma:push` 通过
- `npm --prefix D:/harness-project/server run build` 通过
- `npm --prefix D:/harness-project/app run build` 通过
- Playwright 访问 `http://127.0.0.1:4173/project/576bcf5b-3fe2-4ad4-be18-21319fc07303/s/29218afd-c3fc-4224-98ad-434401948894/simulation`，页面可打开；控制台仅剩既有飞书 CORS 报错，与本任务无关

### 备注:
- `init.ps1` 仍有编码问题，本次继续采用手动 build + preview/server 启动方式验证
- Playwright 直达旧路径 `/project/:id/scenario/:sid/simulation` 会落到 404，当前有效路由为 `/project/:id/s/:sid/simulation`

## [2026-04-11 09:38] - Task: [T15] Simulation 仿真引擎

### 完成的工作:
- 后端新增 Simulation/AnnualDrop 数据模型与接口：`server/prisma/schema.prisma`、`server/src/routes/simulations.ts`、`server/src/routes/annualDrops.ts`
- 新增 `server/src/services/simulationService.ts`，支持仿真任务保存、运行结果快照、转正式场景
- 新增 `server/src/services/annualDropService.ts`，支撑年降记录保存与 impact 查询
- 前端新增 `app/src/lib/simulationApi.ts` 统一访问仿真/年降接口
- 重写 `app/src/pages/SimulationPage.tsx`，补齐保存仿真、已保存任务列表、运行任务、转正式场景 UI
- 重写 `app/src/pages/AnnualDropPage.tsx`，补齐保存年降记录、已保存记录列表、impact 载入能力
- 接入 `server/src/index.ts` 路由注册，并完成 Prisma schema push

### 测试:
- `npm --prefix D:/harness-project/server run prisma:generate` 通过
- `npm --prefix D:/harness-project/server run prisma:push` 通过
- `npm --prefix D:/harness-project/server run build` 通过
- `npm --prefix D:/harness-project/app run build` 通过
- Playwright 访问 `http://127.0.0.1:4173/project/576bcf5b-3fe2-4ad4-be18-21319fc07303/s/29218afd-c3fc-4224-98ad-434401948894/simulation`，页面可打开；控制台仅剩既有飞书 CORS 报错，与本任务无关

### 备注:
- `init.ps1` 仍有编码问题，本次继续采用手动 build + preview/server 启动方式验证
- Playwright 直达旧路径 `/project/:id/scenario/:sid/simulation` 会落到 404，当前有效路由为 `/project/:id/s/:sid/simulation`

## [2026-04-11 10:12] - Task: [T16] 年降管理

### 完成的工作:
- 完善年降页 `app/src/pages/AnnualDropPage.tsx` 的周期保存逻辑，改为一次保存全部年度周期，而不是只落 1 条记录
- 接入 `updateAnnualDrop`，同一年份再次保存时走更新，保证年降是“有周期和影响链路的业务对象”而不是一次性数字
- 补充已保存记录区的查看态提示，并在载入影响时同步选中当前记录
- 将 `task.json` 中 T16 状态更新为 done，并补充 Playwright 验收产物到 `test-screenshots/`

### 测试:
- `npm --prefix D:/harness-project/server run build` 通过
- `npm --prefix D:/harness-project/app run build` 通过
- Playwright 打开 `http://127.0.0.1:4174/project/576bcf5b-3fe2-4ad4-be18-21319fc07303/s/29218afd-c3fc-4224-98ad-434401948894/annual-drop`，页面正常渲染
- 截图 `test-screenshots/t16-annual-drop-page.png`，快照 `test-screenshots/t16-annual-drop-page.md`

### 备注:
- `init.ps1` 仍因编码/字符串解析错误无法直接运行，本次继续以手动 build + server/start + app/preview 完成验收
- 当前前端 preview 需走 `http://127.0.0.1:4174`，避免静态 `python -m http.server` 无法代理 `/api`

## [2026-04-11 10:12] - Task: [T16] 年降管理

### 完成的工作:
- 完善年降页 `app/src/pages/AnnualDropPage.tsx` 的周期保存逻辑，改为一次保存全部年度周期，而不是只落 1 条记录
- 接入 `updateAnnualDrop`，同一年份再次保存时走更新，保证年降是“有周期和影响链路的业务对象”而不是一次性数字
- 补充已保存记录区的查看态提示，并在载入影响时同步选中当前记录
- 将 `task.json` 中 T16 状态更新为 done，并补充 Playwright 验收产物到 `test-screenshots/`

### 测试:
- `npm --prefix D:/harness-project/server run build` 通过
- `npm --prefix D:/harness-project/app run build` 通过
- Playwright 打开 `http://127.0.0.1:4174/project/576bcf5b-3fe2-4ad4-be18-21319fc07303/s/29218afd-c3fc-4224-98ad-434401948894/annual-drop`，页面正常渲染
- 截图 `test-screenshots/t16-annual-drop-page.png`，快照 `test-screenshots/t16-annual-drop-page.md`

### 备注:
- `init.ps1` 仍因编码/字符串解析错误无法直接运行，本次继续以手动 build + server/start + app/preview 完成验收
- 当前前端 preview 需走 `http://127.0.0.1:4174`，避免静态 `python -m http.server` 无法代理 `/api`

## [2026-04-11 11:56] - Task: [T17] 预警规则 CRUD

### 完成的工作:
- 后端新增 `AlertRule` Prisma 模型，并接入 `server/src/routes/alertRules.ts`、`server/src/services/alertRuleService.ts`，补齐 `GET/POST/PUT/DELETE /api/alert-rules`
- 在 `server/src/index.ts` 注册预警规则路由，并完成 Prisma generate + db push，同步数据库结构
- 前端新增 `app/src/lib/alertRuleApi.ts`，统一封装预警规则 CRUD API
- 重写 `app/src/pages/AlertsPage.tsx`，保留原“预警中心”页面，同时新增 `mode="rules"` 的预警规则页，支持规则列表、新建、删除与表单提交
- 更新 `app/src/App.tsx` 和 `app/src/components/Breadcrumb.tsx`，接入 `/settings/alert-rules` 路由与面包屑导航
- 补齐 `app/src/test/setup.ts` 对 `fake-indexeddb/auto` 的加载，并更新根级 `vitest.config.js` 指向该 setup 文件
- 新增验收截图：`test-screenshots/t17-alert-rules-page.png`、`test-screenshots/t17-alert-rules-current.png`、`test-screenshots/t17-alert-rules-form.png`、`test-screenshots/t17-alert-rules-created.png`

### 测试:
- `cd D:/harness-project/server && npx prisma generate && npx prisma db push` 通过
- `cd D:/harness-project/server && npm run build` 通过
- `cd D:/harness-project/app && npm run build` 通过
- `cd D:/harness-project/app && npm run test:run` 未全绿：存在仓库既有失败，与 T17 无直接关系（`src/data/scenarioFork.test.ts` 的 `getScenarioOnetimeCostFallback` 导出不匹配；若干组件测试受 `lottie-web` canvas mock 缺失影响）
- API 冒烟验证：登录后完成 `POST/GET/PUT/DELETE /api/alert-rules` 全链路；浏览器创建规则“回收滞后预警”后列表显示 1 条记录
- Playwright 验证 `http://localhost:5173/settings/alert-rules` 可正常打开、弹出新建规则表单并成功创建规则

### 备注:
- `init.ps1` 仍有编码/解析错误，本次继续以手动启动 server 与 Vite dev 方式完成验收
- 浏览器控制台仅见既有 Semi UI StrictMode `findDOMNode` 警告，不影响本任务验收

## [2026-04-11 12:25] - Task: [T18] 预警事件 + Alerts 中心

### 完成的工作:
- 后端新增 `AlertEvent` 模型，并实现 `server/src/routes/alerts.ts`、`server/src/services/alertEventService.ts`，补齐 `GET /api/projects/:id/alerts`、`GET /api/alerts`、`GET /api/alerts/:eid`、`PUT /api/alerts/:eid`、`GET /api/alerts/summary`
- 更新 `server/src/index.ts`、`server/prisma/seed.ts`，接入预警事件路由并补充 G281 预警演示数据，支持项目级与全局预警中心直接验收
- 前端新增 `app/src/lib/alertEventApi.ts`，重写 `app/src/pages/AlertsPage.tsx`，实现项目预警中心、全局预警中心、分类/严重度/状态筛选、详情弹窗与状态流转
- 更新 `app/src/App.tsx`、`app/src/pages/DashboardPage.tsx`、`app/src/components/Breadcrumb.tsx`、`app/src/layouts/MainLayout.tsx`，接入 `/project/:id/alerts` 路由、项目内快捷入口、面包屑和右上角预警徽章计数
- 修复前端既有测试环境问题：补齐 `app/src/data/e281Fallback.ts` 的 `getScenarioOnetimeCostFallback` 导出，并清理 `app/src/test/setup.ts` 重复引入

### 测试:
- `cd D:/harness-project/server && npm run prisma:generate` 通过
- `cd D:/harness-project/server && npm run prisma:push` 通过
- `cd D:/harness-project/server && npm run seed` 通过
- `cd D:/harness-project/server && npm run build` 通过
- `cd D:/harness-project/app && npm run build` 通过
- `cd D:/harness-project/app && npm run test:run` 仍未全绿：`src/components/*`、`src/store/*` 等 10 个 UI suite 受 Vitest 仍解析旧路径 `D:/成本核算动态模型/app/src/test/setup.ts` 影响，当前任务相关的 `src/data/scenarioFork.test.ts` 已修复通过
- Playwright 验证 `http://127.0.0.1:4173/alerts` 与 `http://127.0.0.1:4173/project/<G281项目ID>/alerts` 可正常渲染，截图 `test-screenshots/t18-alerts-global-page.png`、`test-screenshots/t18-project-alerts-page.png`，快照 `test-screenshots/t18-project-alerts-page.md`

### 备注:
- `init.ps1` 仍因编码损坏无法直接运行，本次继续采用手动启动 server 与 app preview 完成验收
- 浏览器控制台剩余飞书 OAuth 的既有 CORS 报错与苹果 PWA meta 警告，不影响 T18 功能验收

## [2026-04-11 11:56] - Task: [T17] 预警规则 CRUD

### 完成的工作:
- 后端新增 `AlertRule` Prisma 模型，并接入 `server/src/routes/alertRules.ts`、`server/src/services/alertRuleService.ts`，补齐 `GET/POST/PUT/DELETE /api/alert-rules`
- 在 `server/src/index.ts` 注册预警规则路由，并完成 Prisma generate + db push，同步数据库结构
- 前端新增 `app/src/lib/alertRuleApi.ts`，统一封装预警规则 CRUD API
- 重写 `app/src/pages/AlertsPage.tsx`，保留原“预警中心”页面，同时新增 `mode="rules"` 的预警规则页，支持规则列表、新建、删除与表单提交
- 更新 `app/src/App.tsx` 和 `app/src/components/Breadcrumb.tsx`，接入 `/settings/alert-rules` 路由与面包屑导航
- 补齐 `app/src/test/setup.ts` 对 `fake-indexeddb/auto` 的加载，并更新根级 `vitest.config.js` 指向该 setup 文件
- 新增验收截图：`test-screenshots/t17-alert-rules-page.png`、`test-screenshots/t17-alert-rules-current.png`、`test-screenshots/t17-alert-rules-form.png`、`test-screenshots/t17-alert-rules-created.png`

### 测试:
- `cd D:/harness-project/server && npx prisma generate && npx prisma db push` 通过
- `cd D:/harness-project/server && npm run build` 通过
- `cd D:/harness-project/app && npm run build` 通过
- `cd D:/harness-project/app && npm run test:run` 未全绿：存在仓库既有失败，与 T17 无直接关系（`src/data/scenarioFork.test.ts` 的 `getScenarioOnetimeCostFallback` 导出不匹配；若干组件测试受 `lottie-web` canvas mock 缺失影响）
- API 冒烟验证：登录后完成 `POST/GET/PUT/DELETE /api/alert-rules` 全链路；浏览器创建规则“回收滞后预警”后列表显示 1 条记录
- Playwright 验证 `http://localhost:5173/settings/alert-rules` 可正常打开、弹出新建规则表单并成功创建规则

### 备注:
- `init.ps1` 仍有编码/解析错误，本次继续以手动启动 server 与 Vite dev 方式完成验收
- 浏览器控制台仅见既有 Semi UI StrictMode `findDOMNode` 警告，不影响本任务验收
