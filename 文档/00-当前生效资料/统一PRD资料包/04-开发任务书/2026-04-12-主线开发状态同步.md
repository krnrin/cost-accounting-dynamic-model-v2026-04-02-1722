# 2026-04-12 主线开发状态同步

> 基准分支：`codex/phase12-mainline-delivery`
> 基准提交：`5e7d735`
> 状态来源：当前主线 `app/ + server/` 源码、测试、构建结果

## 本轮已完成

- 场景管理
  - 场景新建/编辑弹窗已支持显式绑定 `rateSnapshotVersion`
  - 后端已支持校验已发布 settings 版本，并在创建/更新场景时真正绑定指定费率快照
  - 场景对比页已补 server compare 读取，服务端失败时会回退本地缓存
- 设变与跟踪
  - `ChangeEnginePage` 已补为正式设变台账页
  - 已支持读取场景下的 change events、创建 snapshot、运行 version compare、创建 change event、测算影响并回刷台账
  - 已补稳定组件测试
- 分摊回收跟踪
  - `AllocManagerPage`、`allocStore`、`allocationApi` 已完成 remote-first 第一阶段
  - 服务端已新增场景级 `bulk-sync`，按线束聚合输入拆成 `tooling/testing/rnd` 三类 `AllocationItem`
  - 服务端会同步一条 `latest` 回收快照，前端据此重建分摊汇总和回收进度

## 本轮验证结果

- `npm --prefix app run test:run`
- `npm --prefix app run build`
- `npm --prefix server run build`

以上命令在本轮状态下均已通过。

## 当前仍未收口

- BOM/Univer 主工作台
  - 全屏工作区、语义 diff、表头黄条、严格逐表确认 `KSK -> 二次物料 -> 变更履历` 尚未完整主线化
- BOM 联动闭环
  - BOM 变更到 `ChangeEvent`、版本快照、审计日志的自动贯通尚未完全打通
- 分摊回收第二阶段
  - `scenarioFork/delete` 还未同步远端 allocation/recovery
  - `no-sid` 本地分支仍需继续收口
  - Prisma 层唯一性、审计、增量同步约束尚未补齐
- 报价与价格治理
  - `draft -> confirmed -> published` 状态机还需继续核对前后端一致性
  - 价格差异治理、责任人、Tracking 双向同步仍未完全验收
- 治理链
  - Alerts、VersionPanel、AuditLogPanel、Settings 发布追踪、归档中心仍未形成统一事实源闭环
- 通用化
  - G281/E281 样本硬编码仍需继续下沉到配置层

## 下一批优先项

1. 收口 `scenarioFork/delete` 对远端 allocation/recovery 的复制与删除。
2. 把 BOM/Univer 语义 diff、黄条提示、严格逐表确认完整迁回主线。
3. 把报价 confirm/publish 和价格差异治理接入 Version/Audit/Tracking。
